{% macro check_block (type = 'radio', value = '', subtitle = '', name = '', target = '') %}
  <div class="m-check-block">
    <input type="{{ type }}" name="{{ name }}" id="{{ value | urlencode }}" value="{{ value }}" data-target="{{ target }}">
    <label for="{{ value | urlencode }}">
      <span>
        {{ value }}
        {% if subtitle %}
          <span class="subtitle">{{ subtitle }}</span>
        {% endif %}
      </span>
      <i class="icon2-check"></i></label>
  </div>
{% endmacro %}
